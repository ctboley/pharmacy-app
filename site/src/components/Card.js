import React from "react";

const Card = () => {
  const grid = (
    <>
      <Grid
        container
        spacing={24}
        justify="center"
        style={{ minHeight: "100vh", maxWidth: "100%" }}
      ></Grid>
    </>
  );
  return {
    /* blah */
  };
};

export default Card;
